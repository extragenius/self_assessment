<h1>Delete Question</h1>
<h2><%= @question.title %></h2>
<p>Are you sure you want to delete this question?</p>

<%= form_for(@question, :url => question_path(@question), :method => :delete) do |form| %>
<%= form.submit('Yes') %>
<%= link_to('Cancel', url_for(:back)) %>
<% end %>

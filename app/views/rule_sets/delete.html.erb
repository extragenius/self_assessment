<h1>Delete RuleSet</h1>
<h2><%= @rule_set.title %></h2>
<p>Are you sure you want to delete this rule set?</p>

<%= form_for(@rule_set, :url => rule_set_path(@rule_set), :method => :delete) do |form| %>
<%= form.submit('Yes') %>
<%= link_to('Cancel', url_for(:back)) %>
<% end %>
